import * as React from "react";
import { Theme } from "./types/Theme";
export declare type CustomThemeProvider = React.ComponentType<{
    theme: Theme;
    children: React.ReactNode;
}>;
export declare type ThemesProviderSettings = {
    disableThemePreview?: boolean;
    CustomThemeProvider?: CustomThemeProvider;
};
export declare const DEFAULT_SETTINGS: ThemesProviderSettings;
export declare const withThemesProvider: (themes: Theme[], settings?: ThemesProviderSettings, CustomThemeProvider?: React.ComponentType<{
    theme: Theme;
    children: React.ReactNode;
}>) => (story: any) => JSX.Element;
