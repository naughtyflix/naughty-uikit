import * as React from "react";
import { Theme } from "./types/Theme";
import { ThemesProviderSettings } from "./withThemesProvider";
export interface ThemesProviderProps {
    themes: Array<Theme>;
    settings: ThemesProviderSettings;
    CustomThemeProvider?: React.ComponentType<{
        theme: Theme;
    }>;
    story?: any;
    children: React.ReactChild;
}
export declare const ThemesProvider: React.FunctionComponent<ThemesProviderProps>;
